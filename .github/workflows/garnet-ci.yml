name: Garnet E2B SDK CI
on:
  workflow_dispatch:
  schedule:
    - cron: '30 9 * * *'  # 09:30 UTC daily
jobs:
  python-sdk-install:
    name: Python SDK Install (Garnet)
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    defaults:
      run:
        working-directory: ./packages/python-sdk
    steps:
      - uses: actions/checkout@v4

      - name: Garnet Runtime Visibility
        uses: garnet-org/action@v2
        with:
          api_token: ${{ secrets.GARNET_API_TOKEN }}
          jibril_version: latest

      - name: Parse .tool-versions
        uses: wistia/parse-tool-versions@v2.1.1
        with:
          filename: '.tool-versions'
          uppercase: 'true'
          prefix: 'tool_version_'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '${{ env.TOOL_VERSION_PYTHON }}'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: '${{ env.TOOL_VERSION_POETRY }}'
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true

      - name: Poetry install (resolves + downloads all deps)
        run: poetry install --no-interaction

      - name: Poetry build (creates wheel + sdist)
        run: poetry build --no-interaction

      - name: List installed packages
        run: poetry run pip list

  js-sdk-install:
    name: JS SDK Install (Garnet)
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Garnet Runtime Visibility
        uses: garnet-org/action@v2
        with:
          api_token: ${{ secrets.GARNET_API_TOKEN }}
          jibril_version: latest

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: pnpm install (full monorepo)
        run: pnpm install --no-frozen-lockfile

      - name: Build js-sdk
        working-directory: ./packages/js-sdk
        run: pnpm run build || true
